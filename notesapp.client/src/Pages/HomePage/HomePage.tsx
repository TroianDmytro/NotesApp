import { FC } from 'react';
import { HomePageWrapper } from './HomePage.styled';

const HomePage: FC = () => (
 <HomePageWrapper>
    HomePage Component
 </HomePageWrapper>
);

export default HomePage;
